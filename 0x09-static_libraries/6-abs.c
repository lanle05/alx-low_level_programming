#include "main.h"

/**
 * _abs - find the absolute value of a certain number
 * @i: number under test
 * Return: always 0
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);

}
